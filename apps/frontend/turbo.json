{
  "extends": [
    "//"
  ],
  "pipeline": {
    "build": {
      "outputs": [
        "dist/**"
      ]
    },
    "build-storybook": {
      "outputs": [
        "dist/**",
        "storybook-static/**"
      ]
    }
  }
}